import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Simple entry point for React 17
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
